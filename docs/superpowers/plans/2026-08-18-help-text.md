# Help Text Rewrite Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the flat help example list with a concise, copyable and behavior-accurate administrator guide.

**Architecture:** Keep the existing `help` intent and plain-text delivery path. Rewrite only `HELP_TEXT` and add a contract test that locks required sections, exact command examples and a safe message-length bound.

**Tech Stack:** Python 3, unittest, existing NoneBot intent/service pipeline.

---

### Task 1: Add the failing help contract

**Files:**
- Create: `tests/test_help_text.py`

- [ ] Assert help is grouped into prerequisites, queries, record creation, moderation, state maintenance, deduction policy and confirmation.
- [ ] Assert copyable commands for all policy query/write forms and merged-forward behavior are present.
- [ ] Assert `帮助` routes to the exact public help text and the message stays below 2500 characters.
- [ ] Run `TARGET_GROUP_ID=135792468 .venv/bin/python -m unittest tests.test_help_text -v` and confirm the old flat list fails.

### Task 2: Rewrite HELP_TEXT

**Files:**
- Modify: `plugins/violation_record/formatter.py`

- [ ] Replace the flat examples with the approved grouped manual.
- [ ] Use only commands verified against the current router and fixed policy parser.
- [ ] Run focused and full tests.

### Task 3: Finish v1.0.2.5beta

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `docs/releases/2026-08-18-v1.0.2.5beta.md`

- [ ] Add the help rewrite to the existing v1.0.2.5beta notes.
- [ ] Deploy and restart only the business bot.
- [ ] Stage only v1.0.2.5beta changes, commit, tag and push the current feature branch.
