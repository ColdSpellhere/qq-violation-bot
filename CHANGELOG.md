# 更新日志

本文件记录 QQ 违规记录机器人的公开版本变化。

## [Unreleased]

### 实时关键词内容告警

- 后台受控词库升级为实例私有、原子分代的七分类目录，支持中国政治、淫秽色情、男女对立、争议话题、二次元游戏争议、血腥暴力和恐怖主义；manifest/shard 带来源、版本和 SHA-256，运行时缓存编译结果，坏 generation 保留完整 last-known-good。
- 中国政治分类由代码强制最高保密：任何政治或混合命中都使用固定“政治敏感告警”标题，显示发送者昵称与 QQ 以便定位，但隐藏全部词条、目录分类和正文；若昵称本身包含受保护内容则只保留 QQ。其他后台分类只允许在管理群显示安全分类名、去重命中词与有界摘录。QQ `/违禁词` 命令仍只管理人工规则，无法枚举或探测后台目录。
- 收紧词库启用边界：普通辱骂不再作为性别对立自动告警，宽泛候选保留为 `shadow`；`gender_conflict` 的 active 条目必须携带人工复核标签，导入和回滚均阻止未经复核的旧 generation 再次启用。
- 新增私有目录导入、幂等校验、legacy 合并、pointer 回滚和权限收紧工具；真实词库、构建文件、备份和运行产物不进入 Git、QQ、日志或模型上下文，检测仍不调用 AI，也不触发任何自动处置。
- 新增通用 `plugins.content_alert` 独立插件：默认关闭、不调用 AI/LLM，只检查部署后新实时消息中的 OneBot `text` segment；不回放历史，不读取图片、语音、文件、卡片、引用正文，也不跨 segment 拼接匹配。
- 规则和文本统一执行 Unicode NFKC、`casefold`、去空白及去 `Cf` 格式字符，再进行文字字面子串匹配；正则元字符按普通字符处理，重叠命中优先保留较长规则，同一消息的多个命中合并为一条纯文本告警。
- 新增超级管理员命令 `/违禁词 列表`、`/违禁词 添加 <关键词>`、`/违禁词 删除 <编号>`，以及 `/违禁词告警 开|关` 运行时开关；规则原子写入实例私有 `data/content_alert/keywords.json`，不保存消息正文且不得提交 Git。
- 告警源群必须全部属于 monitor-only 作用域，并与管理报告群及业务群隔离；插件仅向报告群告警，不撤回、禁言、踢人、记录违规或改变业务判断。未完整配置的实例不会注册 matcher 或创建数据文件。
- 当前 MVP 使用 OneBot 直接发送和进程内重复保护，不提供持久化 outbox、自动重试或历史补偿；QQ 离线、发送失败或进程中断可能漏告警，失败只记录脱敏日志，不能报告为成功。后续检测器可在 `content_alert` 边界扩展，但本版只启用字面关键词。
- 人工规则与旧后台回退规则改为实例私有的双文件物理隔离：QQ `/违禁词` 命令只接触人工规则；旧回退规则仍采用全量隐藏。受管分代目录启用后按上方分类披露策略处理。公开扫描器显式拒绝跟踪任何 `data/content_alert/` 运行数据。

### 独立群员监控

- 新增实例级、默认关闭的 monitor-only 群员监控：首次完整同步导出带时间的两列 XLSX 到独立日志群，后续实时记录主动退群与管理员移出；不读取聊天正文，不进入业务、归档、记忆、图片或任何 LLM 链路。
- 首次名单必须与 OneBot 群信息总数完全一致；离线复核使用连续两次缺失、2%/20 人熔断与每轮 20 条上限。大规模差异只有在同一完整成员集合连续出现 3 次后才会原子确认，既避免截断响应误报，也不会让真实多人退群永久卡住。
- 退群成员状态与 outbox 改为同一 SQLite 事务；逐条 10 分钟租约、水位检查和稳定事件编号避免活跃发送者租约过期及重入后的迟到旧事件覆盖新状态。
- 成员快照、导出目标/哈希和退群 outbox 按实例持久化；并发工作者使用可恢复租约，延迟通知按成员离群 episode 去重，旧事件重放不能覆盖重新入群状态。
- Excel 将 QQ号强制保存为文本，并转义不可信群名片/昵称中的公式前缀和非法控制字符；退群日志强制使用 OneBot 纯文本段，群名片不能注入 CQ 码。
- 新增 `/群员监控 开|关` 热开关；监控群即使被误加到聊天白名单，也不能响应模块或记忆治理命令。启用配置会拒绝监控群与业务目标群相同，未配置实例不注册插件或创建数据。

### 聊天文字模型切换与 GLM-4.7-Flash

- 新增超级管理员主命令 `/模型切换 GLM|原模型|状态`，并提供 `/聊天模型` 同义命令；旧命令 `/穷鬼模式 开|关` 保留兼容。切换只作用于后续群聊和私聊纯文字回复，不改变业务意图、成员记忆、私聊事实/摘要、关系状态或其他模型调用。
- 带当前原图的聊天继续使用原视觉模型；群聊和私聊图片下载、恢复、识别、已有图片描述、原文归档、违规证据图片和表情包均保持原配置。聊天文字模型切换也不改变聊天概率、提示词或联网搜索开关。
- GLM 使用独立连接池、超时和实例级私有密钥，并与原供应商共享总并发及各调用域通道；关闭时两边并行回收。配置不完整时拒绝切到 GLM；已持久化为 GLM 但配置在重启时失效时失败关闭，不暗中回退到原付费模型。仅有 GLM 密钥时拒绝切回缺失的原聊天文字模型，避免“切换成功”后聊天全部失败。公开扫描新增 GLM/Tavily 密钥与形态检查，运行时状态和日志均不保存密钥。
- 状态层拒绝“未选中 GLM、又没有原模型密钥却开启模型网关”的无效组合，避免纯 GLM 实例关闭网关后再开启导致下次重启失败。
- 联网搜索仍由 `/联网搜索` 独立控制，不随聊天文字模型选择自动改变；开启时仍会消耗 Tavily 配额。
- 模型网关扩展私聊事实抽取接口，并拒绝多模态内容进入文字专用 GLM；供应商报告截断或上下文超限时按契约失败处理，避免把不完整 JSON 当作成功结果。
- GLM 返回 `1302/1305` 限流或繁忙响应时立即失败，不再无效重试；明确 @ 和白名单私聊遇到临时限流或超时时会返回固定繁忙提示，随机接话继续静默。后台记忆继续遵循原记忆开关和原模型配置，不因聊天文字模型切换而暂停。
- 修复 GLM 偶尔把 fenced JSON 原样发送到 QQ、把其他群友的话归到当前发送者的问题；聊天结果只接受完整且合法的回复契约，Prompt Builder 为当前说话人和萝卜猫增加不可混淆的固定身份锚点。所有 AI 文字统一作为 OneBot 纯文本段发送，模型输出的 CQ 语法不能触发 @、引用或远程图片。
- 运行时关闭群聊、移除私聊权限或治理清空私聊后，在途请求不会再发送过期的回复或繁忙提示；后台记忆任务继续遵守私聊记忆、关系状态及治理边界，不受聊天文字模型选择影响。

### 私聊图片与双实例可靠性修复

- 白名单私聊新增纯图片和图文理解：每次最多读取 4 张、受总字节预算约束；当前原图只在本次请求内存中交给聊天模型，不写群聊图片表、不落盘，持久记忆开启时仅按私聊用户保存受限文字描述。
- 私聊图片描述随原文保留和治理清空一同删除；数据库 schema 升至 v3，迁移会在现有在线备份后幂等增加描述列，不回溯旧图片，也不把模型图片观察及其派生回复抽取为滚动摘要、长期事实、关系状态或业务数据。图文消息仍只允许用户真实文字进入自动记忆。
- 私聊清空、白名单或运行时开关在识图、生成、逐条发送和持久化边界都会重新校验；持久/非持久模式切换会清空进程内兼容队列，避免已治理删除的上下文重新出现。
- LLM Gateway 将 HTTP 402 单独归类为不可重试的 `payment_required`，日志与使用统计不保存供应商响应正文；群聊图片恢复只处理有限时间窗口和有限数量的新任务，历史事件也不能从实时入口绕过限制，不再在充值或重启后集中回补历史失败图片。
- 发布目录新增 Git commit/tree/源码摘要清单校验；同 SHA 目录被修改或加塞文件时拒绝复用。成功切换会持久保存实例自己的 `previous` 指针，同 SHA 重试不会覆盖它；发布和旧版本清理共用排他锁，并同时保护两个实例的 `current` 与 `previous`。
- 生产入口和发布目录内迁移统一禁写 Python 字节码，避免 `__pycache__` 污染只读发布树；迁移工具直接安全读取实例 `.env`，不再要求 shell `source`。

### 聊天联网搜索与多段回复

- 新增默认关闭的 Tavily 聊天联网搜索，仅允许白名单私聊和群内明确 @ 使用；普通随机接话及全部业务判断保持隔离。
- 新增 `/联网搜索 开|关` 运行时开关，搜索失败自动降级且不泄露密钥或聊天历史。
- 私聊和明确 @ 回复支持模型按需要输出最多 3 条连续消息；随机接话仍限制为 1 条，表情包只附在最后一条。
- 搜索查询仅来自当前消息且限制 200 字，结果最多 5 条/4000 字并作为不可信聊天数据处理；不新增数据库表。

### CArroT / kona 双实例部署

- 增加同机双实例路径隔离、永久纯聊天模式、目标机器人管理寻址、原子部署回滚、NapCat 运行目录隔离、2 GiB Swap 工具，以及 CArroT 候选到 kona 人工审批的发布门禁。

### 统一模型网关与提示构建

- 新增统一 LLM Gateway，为业务意图、聊天、成员记忆、私聊摘要/关系和图片描述提供命名调用接口；各领域继续保留独立提示词和返回契约，聊天模型不能执行群管理业务。
- 共享异步连接池、总并发和分任务并发通道，统一有限重试、超时、错误分类、有界关闭和日志脱敏；取消请求不会被包装或继续重试。
- 新增 `llm_usage_events` 使用统计，记录任务、模型、token、延迟、状态、重试次数和错误类别，不记录请求正文、模型响应、密钥或图片；暂不估算供应商费用。
- 新增可单测的聊天 Prompt Builder。固定安全规则保留在 system 层，人设、上下文、成员事实、关系、未完话题和图片描述均作为带标签的不可信数据；按最终转义后的字符数执行确定性预算。
- 修复 Prompt Builder 可能把其他群友的第一人称或成员事实归到当前发送者的问题：按精确 QQ号生成请求级 `speaker_ref`，同时绑定历史、记忆、@、引用和当前消息；可用 `/提示构建 关` 立即切回旧提示路径，不涉及数据库迁移。
- 模型网关总开关、视觉/私聊记忆/成员记忆/聊天/业务子开关和 Prompt Builder 开关均默认关闭并可由超级管理员热切换。关闭后后续请求恢复原路径，无需代码或数据库回滚。
- 图片、成员记忆、私聊摘要/关系、群聊/私聊聊天和业务意图按低风险到高风险顺序迁移；业务迁移只替换传输，保留原快捷规则、固定业务提示、JSON 提取和后端校验。
- 私聊关系的四字段网关结果统一按不确定状态处理，避免把模型推测当作确定事实；关闭网关时仍保留旧关系 certainty 契约和原有截断行为。
- 公开仓库扫描新增 SQLite、运行时功能状态、导出文件、图片和日志路径检查，并继续扫描当前树和完整历史中的密钥、私有运行值及二进制内容。

### 私聊连续性记忆与治理

- 新增默认关闭的白名单私聊持久记忆，只处理部署后新到达的允许用户私聊，不回溯历史；原文、摘要、长期事实、关系状态和未完话题按用户隔离。
- 原文默认每用户最多 500 条并保留 30 天，限制可配置；显式迁移和服务自动迁移的含正文备份统一写入 `backups/private_memory/` 并使用同一保留天数。服务启动时清理一次，之后每 24 小时清理两类精确命名的过期普通单链接备份，即使不再运行迁移也会到期删除；其他文件、符号链接和硬链接保留。删除事务启用 `secure_delete` 并尝试 WAL checkpoint，不自动执行 `VACUUM`。
- 治理清空发生 checkpoint busy 时，逻辑清理仍返回成功并写入可检索的持久审计；每日保留清理发生 busy 时仅记录脱敏日志并在后续周期重试，不写治理审计。
- 新增超级管理员 `/记忆` 治理命令：内容和预览仅私发；写操作使用绑定操作者和内容、10 分钟有效、只能确认一次的操作码，并要求填写确认原因。
- 新增显式迁移脚本，支持只读预检和 `--apply`；对现有数据库先创建并验证 SQLite 在线备份，再执行幂等 schema 迁移。私聊持久记忆、关系状态和治理入口均保持安全默认关闭，可分别止损。
- 公开仓库扫描支持仅针对一个已审计的历史合成测试 fixture blob 的精确 path/blob/finding-class 基线；当前树不使用任何豁免，其他内容仍按普通泄漏处理。

### 群聊图片理解

- 新增默认关闭的实时群聊图片识别：仅处理已开放聊天的允许群中部署后新到达的真人图片，不回扫历史归档；一条消息的每张图片独立生成事实性中文描述。
- 聊天原图仅保存于 `data/chat_vision/images/`，保留 7 天后清理；文字描述、哈希和审计信息永久保留。视觉调用复用现有 AI 配置与同一 API Key，不增加独立密钥。
- 未艾特纯图片继续按群聊概率回复；明确艾特机器人且含图片时必答并使用当前可用原图。视觉数据与 `evidence/`、证据数据库及违规证据流程硬隔离。

### 模块化功能控制

- 新增业务、聊天、群聊和私聊的持久化运行时开关，以及群聊群/私聊用户白名单；新安装默认只开启业务，聊天入口默认关闭。
- 仅 NoneBot `SUPERUSERS` 可通过 QQ 命令查看状态、切换模块和维护白名单。运行时状态写入 Git 忽略的 `data/runtime_features.json`，有效状态优先于 `.env` 默认值。
- 业务群与聊天群路由隔离：聊天白名单中的非业务群不进入业务判断；群聊归档和成员记忆受聊天总开关、群聊子开关和群白名单共同约束。
- v1.0.2beta 策略提醒在业务关闭、QQ/OneBot 离线或发送失败时保留，恢复后以一次合并转发概览处理，发送成功前不标记完成。
- 保留 `TARGET_GROUP_ID`、`RANDOM_CHAT_ENABLED`、`PRIVATE_CHAT_ENABLED` 和 `PRIVATE_CHAT_ALLOWED_USER_ID` 作为首次启动/升级兼容输入；新私聊白名单使用 `PRIVATE_CHAT_ALLOWED_USER_IDS`。

### 群友记忆

- 原始特性和历史昵称改为追加式永久账本，不再因 8 条兼容视图上限淘汰。
- 每 5 条新特性生成滚动摘要，聊天只读取摘要与有界的新特性。
- 迁移当前仍保留的记忆，不回溯或重新提炼历史聊天。

## [v1.0.3] - 2026-08-18

### 正式发布

- 将 `v1.0.2beta` 至 `v1.0.2.7beta` 的长期验证成果合并为首个稳定正式版。
- 完整纳入减数策略、上下文闲聊、保守成员记忆、成员查询防刷屏修复、合并转发查询、分组帮助、萝卜猫人格、表情包及独立私聊插件。
- 萝卜猫明确为角色名字而非猫系身份，不自称猫或使用“喵”；“反二梦女”是兴趣和自我标签，不是另一个名字。
- 私聊允许配置逗号分隔的多个精确 QQ号白名单，仍与群业务、聊天归档、成员记忆和违规数据库隔离。

### 稳定性与兼容性

- 正式版继续保留随机群聊、非业务点名兜底、表情包、私聊和减数策略的独立开关，出现异常时可单独关闭。
- 查询两条及以上记录使用合并转发；发送失败时只降级为一条合并文字，避免逐条刷屏。
- 从 `v1.0.2.7beta` 升级无需迁移业务数据；从 `v1.0.1` 或更早版本启用减数策略前，仍须执行 `v1.0.2beta` 的数据库副本预演、迁移和回滚校验。
- 正式发布前完成 239 项自动化回归、Python 编译检查、公开仓库扫描及生产 OneBot 连接检查。

## [v1.0.2.7beta] - 2026-08-18

### 新增

- 新增与群业务完全隔离的单账号私聊插件，代码默认关闭，生产允许账号仅保存在私有 `.env`。
- 允许账号的每条普通文字私聊都由萝卜猫回答，不使用群聊 3% 概率；其他账号、空消息、命令和纯图片静默忽略。
- 私聊保留最近 20 条双方文字作为进程内上下文，服务重启即清空，不写群归档、成员记忆或业务数据库。
- 私聊回复沿用现有 20% 表情包概率和 10% / 90% 条件权重，文字与图片在同一条私聊消息中发送。

### 隔离与回滚

- 私聊插件不导入违规业务、证据、减数或群管理处理器；AI 和发送失败只记错误类型。
- 设置 `PRIVATE_CHAT_ENABLED=false` 并重启机器人即可关闭私聊入口，群机器人不受影响。

## [v1.0.2.6beta] - 2026-08-18

### 新增

- 闲聊人格调整为“萝卜猫”：自然可爱、喜欢花和植物，只在合适的身份玩笑里偶尔自称“反二梦女”。
- 普通群消息随机接话概率调整为 3%，继续读取最近 30 分钟最多 20 条上下文和保守成员记忆。
- 新增非业务点名闲聊兜底：点名或回复机器人时先识别业务，只有 `unknown` 才交给萝卜猫回答。
- 非业务点名兜底提供独立的 `RANDOM_CHAT_DIRECT_FALLBACK_ENABLED` 开关，安全默认关闭，生产显式开启。
- 成功闲聊回复以 20% 概率在同一条消息中附带一张表情包；指定首图占条件选择的 10%，其余 57 张均分 90%。
- 成员记忆改为独立实时微批提炼，不再依赖随机接话是否触发。

### 隔离与回滚

- 查询、记录、减数、禁言等已识别业务保持最高优先级，业务回复不附带表情包。
- 表情包和真实成员记忆只保存在服务器 `data/` 运行目录，不提交到 GitHub。
- 异常时可单独关闭非业务点名兜底、表情包或全部随机闲聊，无需回滚代码或数据库。

## [v1.0.2.5beta] - 2026-08-18

### 新增

- 两条及以上的成员或分区查询结果改为一张 QQ 合并转发卡片，每条记录作为独立节点。
- 证据图片保留在所属记录的节点内，文字与图片顺序不变。
- 单条查询结果继续使用普通图文消息，无记录和导出回复保持现状。
- 重写“帮助”为分组管理手册，按查询、新增记录、群禁言、状态维护、减数策略和确认流程提供可直接复制的命令。
- 帮助中明确昵称模糊查询、减数命令只接受 QQ号、群禁言立即执行，以及其他写操作的二次确认边界。

### 容错与回滚

- 合并转发失败时只发送一条合并文字，不会降级为逐条记录或逐张图片，避免失败路径刷屏。
- 不修改数据库、查询 SQL、排序、记录上限或证据存储；无数据迁移，回滚只需恢复查询发送层和帮助文本。

## [v1.0.2.4beta] - 2026-08-18

### 修复

- 修复 `查询 <群> <名字>` 和 `查询 <群> <名字>违规记录` 错误降级成分区记录查询的问题，避免成员名未匹配时刷出最近 20 条群记录。
- 成员名无法匹配时返回现有的单条未找到提示；匹配到多人时继续显示候选成员。
- 未提供成员名的 `查询 <群> 违规记录` 保持原有分区查询行为。

### 兼容性与回滚

- 不修改数据库、记录格式、成员模糊匹配阈值、随机聊天或成员记忆模块。
- 本版本无数据迁移；如需回滚，恢复 `plugins/violation_record/ai_router.py` 及对应测试即可。

## [v1.0.2.3beta] - 2026-08-06

### 新增

- 群聊上下文新增发送者 QQ、消息 ID、艾特对象、引用消息和引用对象，明确区分群友之间的对话方向。
- 新增保守群友记忆：按群号和 QQ号保存当前昵称、有限历史别名，以及由本人明确原话支持的稳定非敏感特性。
- 记忆使用聊天归档 SQLite 中的独立表，并在 `data/member_memory/` 生成权限为 `600` 的 JSON 镜像，便于服务器本地检查。
- AI 提炼结果必须关联同一成员的真实证据消息及逐字引句；第三方评价、玩笑、推测、临时情绪和敏感信息会被拒绝。

### 行为与隐私

- 成员记忆改为独立于随机回复概率持续收集：同一成员累计 5 条合格消息，或首条待处理消息等待 60 秒，任一条件满足即触发一次微批提炼。
- 当前生产运行配置的随机回复概率为 1%；无法判断消息是否在对机器人说时优先静默。
- GitHub 只保存功能代码、目录规则和格式说明；真实 QQ号、昵称及成员特性继续由 `.gitignore` 排除。
- 记忆仍只接受本人明确原话支持、可回溯且非敏感的稳定特性；提炼失败只记录 warning，不影响随机回复、消息归档、违规记录或群管理模块。

### 回滚

- 上线前备份聊天归档数据库；旧版本会忽略新增记忆表及本地 JSON 文件。
- 可关闭 `RANDOM_CHAT_ENABLED` 或切回 `v1.0.2.2beta`，无需修改违规记录数据库。

## [v1.0.2.2beta] - 2026-08-06

### 新增

- 随机 AI 回复在触发时读取当前群最近 30 分钟内最多 20 条纯文本，按时间顺序理解话题后自然接话。
- 上下文按群名片、QQ 昵称、QQ号的优先级标注成员；当前消息、机器人消息、空消息和 `/` 命令会被过滤。
- 默认随机回复概率由 5% 调整为 10%。

### 兼容性与回滚

- 不修改聊天归档表结构，不读取图片、违规记录、证据、管理员或减数策略数据。
- 归档读取失败降级为无上下文回复；AI 或 OneBot 发送失败只记录 warning，不影响其他模块。
- 可将概率恢复为 5%、关闭功能或切回 `v1.0.2.1beta`；无需数据库回滚。

## [v1.0.2.1beta] - 2026-08-06

### 新增

- 新增独立、默认关闭的随机 AI 群聊回复插件，仅对当前 `TARGET_GROUP_ID` 生效。
- 对合格的普通成员文字按默认 5% 概率调用现有 OpenAI-compatible AI 接口，回复控制为简短中文闲聊。

### 兼容性与回滚

- 不修改违规记录、聊天归档、群管理、证据或减数策略的数据流和数据库结构。
- 忽略机器人自身消息、@ 机器人消息、空消息和 `/` 开头命令；AI 异常只记录 warning，不向群内发送错误。
- 可通过 `RANDOM_CHAT_ENABLED=false` 立即停用；本版本无数据库迁移。

## [v1.0.2beta] - 2026-08-02

### 新增

- 新增独立的 v1.0.2beta 减数策略引擎，使用事件账本、周期状态、待办和通知队列记录全部自动及人工变化。
- 新增普通 14 天周期、逐次增加 7 天的减缓周期、人工减停、最后警告恢复及违规移出提醒。
- 新增固定管理命令：`减停`、`清除减停`、`续期减停`、`拒绝减停建议`，写操作继续使用原有二次确认。
- 新增减数状态、减缓名单、减停名单、减停建议、待办和日志查询；名单完整显示，不静默截断。
- 新增离线策略结算与持久化通知队列。NapCat 离线不阻止周期结算，恢复连接后补发通知。
- 通知队列新增发送租约回收、发送前有效性复核和逐次尝试历史，已撤销事件或已处理待办不会继续发送。
- 基线迁移产生的周期初始化只保留审计，不进入群通知；真实通知每分钟最多发送 10 条。
- 状态变化新增持久化联动作业；策略瞬时失败不回滚既有业务操作，后台按幂等键自动补做。
- 支持将质询、最后警告和终止状态显式关联到被引用的原始违规记录；撤回误记录时关联状态与原计时同步回退，独立人工状态不受影响。
- 周报新增“减数策略日志”“减数待办”“通知发送历史”和“状态联动作业”工作表，原有工作表保持不变。
- 新增专用基线迁移、预演和逻辑回滚工具；准确表只校准当前次数，不伪造历史违规记录。
- 未覆盖旧成员使用惰性校准快照，查询保持原计数且不启动周期，首条新增禁言才触发策略。
- 逻辑回滚覆盖运行期动态成员，并从 SHA-256 匹配的切换前备份恢复旧计时字段。
- 迁移会识别最后警告后的存量禁言并生成移出待办，避免误触发 90 天自动恢复。
- 撤回或补录后的重放会重建迁移基线周期，并在逻辑回滚时保留上线后合法记录形成的新计时。

### 规则

- 普通周期为 14 天；当前次数达到 3 时自动进入减缓，减缓周期按 21、28、35 天依次递增。
- 减缓期第二条轻度禁言延长 7 天，第三条轻度禁言或任意严重禁言生成减停建议，由管理组决定。
- 普通减停每 30 天到期后等待人工决定；未决定时每小时提醒，不由系统擅自解除。
- 最后警告进入独立 90 天周期；期间新增任何禁言记录都会提醒管理组按斩杀标准决定是否移出。
- 成功执行减数的操作次数最多为 5 次；单次减数值保持为 1，最后警告成功恢复时请求减数值为 2。

### 兼容性与安全边界

- 功能默认关闭。`DEDUCTION_POLICY_V102_ENABLED=false` 时继续运行原每周减计；启用后旧引擎停止结算，避免重复减数。
- 不改变现有 NLP、记录预览、确认、查询文本、查询排序、证据图片和成功回复逻辑。
- 只处理 `TARGET_GROUP_ID`；目标群外消息不进入策略、NLP、数据库、归档、图片下载或自定义日志。
- 新策略表全部使用 `v102_` 命名空间，不复用 2026-07-16 的遗留实验表。
- 撤回通过事件回放恢复标签、周期、减数值和操作次数；补录只重算允许范围内的最近周期。
- 人工减停会绑定触发建议的具体事件；对应误记录被撤回时，相关标签和计时可沿因果链回退。
- 已退群、已移出或已拉黑时停止计时和自动操作，但保留原标签、计数与历史。
- 人工写操作的成功、失败、取消和过期均记录操作人、事由及操作前后状态。
- 本版本为在线长期验证用 beta。启用前必须完成准确表 dry-run、数据库副本迁移及回滚演练，并保留上线快照。

## [v1.0.1] - 2026-07-31

### 新增

- 新增违规记录格式不完整时的中文纠正模板。
- 一次列出已确定的全部缺失或不明确字段，并生成可直接复制的标准单行记录。
- 在缺失位置使用 `<QQ号>`、`<时间，24小时制，如03:30或15:30>`、`<违规行为>` 等明确占位符。
- 补充分区、QQ号、时间、处理人、记录人和备注的填写或自动填充说明。

### 兼容性

- QQ号仍为条件必填：昵称能够唯一匹配现有成员时，可以不填写 QQ号。
- 引用消息时间能够正常取得时，继续沿用原有自动填充逻辑。
- 记录人继续自动取当前发送者；未填写处理人时继续默认为记录人；未填写备注时继续默认为“无”。
- 不改变 NLP 抽取、昵称模糊搜索、查询、确认、入库、历史数据和证据图片逻辑。
- 格式纠正回复不会创建待确认操作、违规记录、成员或管理员数据。

### 验证

- 自动化测试：59 项全部通过。
- 查询文本与排序契约、旧记录证据兼容、目标群外消息隔离和公开仓库安全扫描均通过。
