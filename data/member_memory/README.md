# Member memory runtime directory

The bot writes private runtime profiles under `<group_id>/<user_id>.json` in this directory.
Only this README and `.gitignore` are tracked. Never commit generated profiles: they contain QQ identifiers, nicknames, and member traits.
